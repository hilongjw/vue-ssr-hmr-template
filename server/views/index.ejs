<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <% if (NODE_ENV !== 'development') { %>
        <link rel="stylesheet" href="css/<%= bundle %>.css">
    <% } %>
  </head>
  <body>
    <div id="app"></div>
    <script src="js/<%= bundle %>.js"></script>
  </body>
</html>
